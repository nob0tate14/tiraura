pythonの機械学習のメモ

環境：Windows10のVirtualBoxのUbuntu18.04LTSのanacondaのjupyter-notebook  

ネタがないので機械学習の勉強をしてみる。  
オライリーの本に沿って進める。  

ubuntuのコマンドラインから  
conda activate  
anaconda-navigator  
anacondaの画面が起動するのでjupyter-notebookWINDOWSをlaunchする。  

途中までは手で入力していたが途中でめんどくなったのでgithubからコピペ・・・  
https://github.com/amueller/introduction_to_ml_with_python

本にmglearnをimportしろと書いてあるが先に進む。  
散布図のグラフ表示でmglearnが無いってエラーが出た。  
which pipしたらanacondaがpip持ってたので  
pip install mglearnした。    
importを適当に書いて再実行し、グラフが表示された。  
コピペしまくったので読み合わせないと意味が分からん。  
